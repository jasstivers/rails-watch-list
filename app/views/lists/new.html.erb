<h1>Create a New List</h1>
<%= simple_form_for(@list) do |f| %>
  <%= f.input :name, class: "mb-4", required: false %>
  <div class="mt-3">
    <%= f.button :submit, "Create list", class: "btn btn-primary" %>
  </div>
<% end %>

<%= link_to "Back to Lists", lists_path %>
